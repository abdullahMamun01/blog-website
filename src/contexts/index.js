import { createContext } from "react";

const AuthContext = createContext(null)

const PortalContext = createContext();

const BlogContext = createContext()
//is delete
export {AuthContext,PortalContext,BlogContext}